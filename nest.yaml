vault:
  type: file
  metadata:
    path: secrets

services:
  defo:
    include: example.yml
registry:
  default:
    host: rg.fr-par.scw.cloud/rwsapps
    username: nobody
    password: {{ vault.default_registry_key }}